# 🔄 Инструкция по перезапуску для применения изменений

## ⚠️ Важно: Для применения изменений во Frontend нужно перезапустить dev сервер!

### Что было исправлено:
1. ✅ Кнопка "Подробнее" в галерее теперь работает корректно
2. ✅ Исправлен конфликт между drag-событиями карточки и кликом на кнопку
3. ✅ Добавлена правильная обработка событий (stopPropagation)
4. ✅ Улучшена навигация на профиль

---

## 📋 Как перезапустить компоненты:

### Вариант 1: Перезапуск только Frontend (если изменялся только frontend)

1. **Найдите окно PowerShell с Frontend dev сервером**
   - Оно должно показывать "Frontend Dev Server" или "npm run dev"

2. **Остановите сервер:**
   - Нажмите `Ctrl + C` в этом окне

3. **Запустите заново:**
   ```powershell
   cd frontend
   npm run dev
   ```

### Вариант 2: Полный перезапуск всех компонентов

1. **Закройте все окна PowerShell с запущенными сервисами**

2. **Запустите заново через скрипт:**
   ```powershell
   .\start.ps1
   ```

   Или вручную:
   ```powershell
   # Окно 1: FastAPI
   .\venv\Scripts\Activate.ps1
   uvicorn api.main:app --host 0.0.0.0 --port 8000 --reload

   # Окно 2: Telegram Bot
   .\venv\Scripts\Activate.ps1
   python main.py

   # Окно 3: Frontend
   cd frontend
   npm run dev
   ```

---

## 🔍 Как проверить, что изменения применились:

1. **Откройте браузер** и перейдите на `http://localhost:5173`
2. **Откройте консоль разработчика** (F12)
3. **Перейдите в галерею** и нажмите на кнопку "Подробнее"
4. **В консоли должны появиться логи:**
   - `Button clicked, calling onProfileClick`
   - `Navigating to profile: [telegram_id]`
5. **Должен произойти переход на страницу профиля**

---

## ⚡ Быстрая проверка:

После перезапуска frontend dev сервера:
- ✅ Кнопка "Подробнее" должна работать
- ✅ При клике должен происходить переход на профиль
- ✅ В консоли браузера должны быть логи навигации

---

## 🐛 Если кнопка все еще не работает:

1. **Проверьте консоль браузера** (F12) на наличие ошибок
2. **Убедитесь, что frontend dev сервер перезапущен**
3. **Попробуйте жесткую перезагрузку страницы:** `Ctrl + Shift + R` (или `Cmd + Shift + R` на Mac)
4. **Очистите кэш браузера** если проблема сохраняется

---

**Примечание:** Backend (FastAPI и Telegram Bot) обычно не требуют перезапуска при изменении frontend кода, но если вы изменяли backend код - перезапустите соответствующие сервисы.

