# 🔧 Исправления в API для отправки сообщений

## ✅ Что было исправлено:

### 1. **Проверка debug-user**
   - Добавлена проверка, что запрос не отправляется от "debug-user"
   - Если пользователь открыл приложение не через Telegram, будет понятная ошибка

### 2. **Улучшена обработка ошибок Telegram API**
   - Добавлено логирование ответа от Telegram API
   - Парсинг ошибок от Telegram для более детальной информации
   - Обработка сетевых ошибок (aiohttp.ClientError)

### 3. **Улучшено логирование**
   - Логирование поиска пользователей в Airtable
   - Логирование ответа от Telegram API
   - Детальные сообщения об ошибках

## 🚨 Проблема с "debug-user"

**Если вы тестируете в браузере (не через Telegram):**
- `currentUserId` будет равен `"debug-user"`
- API вернет ошибку 400 с понятным сообщением
- **Решение:** Откройте приложение через Telegram Mini App

## 🔍 Как проверить работу:

### 1. Проверьте логи FastAPI:

В консоли где запущен FastAPI должны появиться логи:
```
Looking for users: from=..., to=...
Found records: from=1, to=1
Sending partnership message to user ... from ...
Telegram API response: status=200, body=...
Telegram message sent successfully to user ...
```

### 2. Если ошибка:

Проверьте логи - там будет детальная информация:
- Если пользователь не найден в Airtable
- Если ошибка отправки в Telegram
- Если проблема с сетью

## 📝 Важно:

1. **Откройте приложение через Telegram** - не через браузер напрямую
2. **Проверьте логи FastAPI** - там будет детальная информация об ошибках
3. **Убедитесь, что пользователи есть в Airtable** с правильными TelegramID

## 🔄 Перезапустите FastAPI:

```powershell
# Остановите текущий процесс (Ctrl+C)
.\venv\Scripts\Activate.ps1
uvicorn api.main:app --host 0.0.0.0 --port 8000 --reload
```

После перезапуска попробуйте снова отправить запрос и проверьте логи!

