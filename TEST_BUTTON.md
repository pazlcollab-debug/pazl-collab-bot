# 🔍 Тестирование кнопки "Предложить партнерство"

## ⚠️ ВАЖНО: Перезапустите Frontend!

1. **Остановите frontend dev сервер** (Ctrl+C в окне с `npm run dev`)
2. **Запустите заново:**
   ```powershell
   cd frontend
   npm run dev
   ```
3. **Обновите страницу в браузере:** `Ctrl + Shift + R` (жесткая перезагрузка)

## 🔍 Проверка в консоли:

### 1. Проверьте, что кнопка рендерится:

В консоли должны появиться логи:
```
🔍 Button render check: {
  isOwnProfile: false,
  hasRequestedPartnership: false,
  partnershipStatus: null,
  shouldShow: true,
  currentUserId: "...",
  telegram_id: "..."
}
```

**Если `shouldShow: false`** - проверьте значения:
- `isOwnProfile` должен быть `false` (не свой профиль)
- `hasRequestedPartnership` должен быть `false` (еще не отправляли)
- `partnershipStatus` должен быть `null` или не `"sent"`

### 2. Нажмите кнопку мышью:

Должны появиться логи:
```
🔵 Mouse down on button
🔵🔵🔵 BUTTON CLICKED! Event: ...
🚀🚀🚀 handlePartnershipRequest CALLED!
```

### 3. Если логи не появляются:

**Проблема:** Кнопка не реагирует на клик

**Решение:**
1. Проверьте, что кнопка видна на странице
2. Проверьте, что нет ошибок в консоли (красным цветом)
3. Попробуйте кликнуть прямо на текст кнопки
4. Проверьте Network tab - отправляется ли запрос?

### 4. Если появляется `🔵 Mouse down` но не `BUTTON CLICKED`:

**Проблема:** onClick не срабатывает

**Решение:**
- Возможно, что-то перекрывает кнопку
- Проверьте z-index в DevTools
- Попробуйте кликнуть в разных местах кнопки

### 5. Если появляется `BUTTON CLICKED` но не `handlePartnershipRequest CALLED`:

**Проблема:** Функция не вызывается

**Решение:**
- Проверьте, нет ли ошибок JavaScript в консоли
- Убедитесь, что frontend dev сервер перезапущен

## 🧪 Тест через консоль:

Выполните в консоли браузера:

```javascript
// Найти кнопку
const btn = document.querySelector('button[type="button"]');
console.log('Button found:', btn);

// Проверить обработчики
console.log('Button onclick:', btn.onclick);
console.log('Button attributes:', btn.attributes);

// Попробовать вызвать через React
const reactKey = Object.keys(btn).find(key => key.startsWith('__reactFiber'));
if (reactKey) {
  console.log('React fiber found');
}

// Попробовать кликнуть программно
btn.click();
```

## 📋 Чеклист:

- [ ] Frontend dev сервер перезапущен
- [ ] Страница обновлена (Ctrl+Shift+R)
- [ ] В консоли есть логи `🔍 Button render check`
- [ ] `shouldShow: true` в логах
- [ ] Кнопка видна на странице
- [ ] Нет ошибок в консоли
- [ ] При клике появляются логи `🔵🔵🔵 BUTTON CLICKED!`

